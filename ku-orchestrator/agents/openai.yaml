interface:
  display_name: "Ku Orchestrator"
  short_description: "Run an isolated-agent development pipeline"
  default_prompt: "Use $ku-orchestrator to resolve the supplied task, plan document, or Jira ticket, then plan, implement, review, fix, and report it with fresh isolated agents."
